# utils.py
import pandas as pd
import numpy as np

def process_hospitalizations(hosp):
    """
    Process the hospitalization dataframe
    
    Args:
        hosp (pd.DataFrame): hospitalization dataframe
    Returns:
        h (np.array): (days,)-sized array of hospitalizations
    """
    
    pass
    # return h



def process_cases_deaths(c_d):
    """
    Process the cases and deaths dataframe
    
    Args:
        c_d (pd.DataFrame): cases and deaths by cause dataframe
    Returns:
        c (np.array): (days,)-sized array of confirmed cases
        d (np.array): (days,)-sized array of deaths
    """    
    pass
    # return c, d